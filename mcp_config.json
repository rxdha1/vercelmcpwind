{
  "project": {
    "name": "vibe-coding-repo",
    "description": "Repo for AI-powered UI/code generation with v0.dev, deployed on Vercel, with database support"
  },
  "providers": {
    "deployment": {
      "type": "vercel",
      "projectId": "vercel_project_id_placeholder",
      "teamId": "team_id_placeholder",
      "token": "VERCEL_TOKEN_PLACEHOLDER"
    },
    "ai": {
      "type": "v0.dev",
      "apiKey": "V0_DEV_API_KEY_PLACEHOLDER"
    },
    "databases": [
      {
        "type": "postgresql",
        "provider": "vercel_postgres",
        "connectionStringEnv": "DATABASE_URL"
      },
      {
        "type": "mysql",
        "provider": "planetscale",
        "connectionStringEnv": "MYSQL_URL"
      },
      {
        "type": "sqlite",
        "provider": "local",
        "filePathEnv": "SQLITE_PATH"
      },
      {
        "type": "mongodb",
        "provider": "mongodb_atlas",
        "connectionStringEnv": "MONGODB_URI"
      }
    ]
  },
  "features": {
    "autoDeploy": true,
    "aiCodeGeneration": true,
    "databaseProvisioning": true,
    "secretsManagement": true,
    "multiEnvironment": true
  },
  "secrets": {
    "VERCEL_TOKEN": "VERCEL_TOKEN_PLACEHOLDER",
    "V0_DEV_API_KEY": "V0_DEV_API_KEY_PLACEHOLDER",
    "DATABASE_URL": "DATABASE_URL_PLACEHOLDER",
    "MYSQL_URL": "MYSQL_URL_PLACEHOLDER",
    "SQLITE_PATH": "SQLITE_PATH_PLACEHOLDER",
    "MONGODB_URI": "MONGODB_URI_PLACEHOLDER"
  },
  "environments": {
    "development": {
      "env": {
        "DATABASE_URL": "postgres://dev_user:dev_pass@localhost/dev_db",
        "DEBUG": "true"
      },
      "secrets": {
        "V0_DEV_API_KEY": "DEV_V0_API_KEY_PLACEHOLDER"
      }
    },
    "staging": {
      "env": {
        "DATABASE_URL": "postgres://stage_user:stage_pass@staging.host/stage_db"
      },
      "secrets": {
        "V0_DEV_API_KEY": "STAGING_V0_API_KEY_PLACEHOLDER"
      }
    },
    "production": {
      "env": {
        "DATABASE_URL": "postgres://prod_user:prod_pass@prod.host/prod_db"
      },
      "secrets": {
        "V0_DEV_API_KEY": "PROD_V0_API_KEY_PLACEHOLDER"
      }
    }
  },
  "instructions": [
    "Use v0.dev for UI and code suggestions.",
    "Deploy all changes to Vercel automatically.",
    "Provision and connect to the database using the provided connection string.",
    "Use secrets management for API keys and sensitive data.",
    "Override environment variables per environment as needed."
  ],
  "mcpServers": {
    "puppeteer": {
      "command": "npx",
      "args": [
        "-y",
        "@modelcontextprotocol/server-puppeteer"
      ],
      "address": "http://localhost:4001"
    },
    "figma": {
      "command": "npx",
      "args": [
        "figma-mcp"
      ],
      "env": {
        "FIGMA_API_KEY": "figma_api_key_placeholder"
      },
      "address": "http://localhost:4002"
    }
  }
}